/*B5) ДВЕ ОДИНАКОВЫЕ ЦИФРЫ РЯДОМ
Ввести целое число и определить, верно ли, что в его записи есть две одинаковые цифры, стоящие рядом.
Input format: Одно целое число
Output format: Единственное слов: YES или NO*/

#include <stdio.h> 

int main(void)
{
    /* Алгоритм:
1. Вводим число.
 2. Если число отрицательное, преобразуем его в положительное (или обработаем знак, но по условию просто целое число, так что можем взять модуль).
 3. Специальный случай: если число меньше 10, то у него нет двух цифр, тем более стоящих рядом -> NO.
 4. В цикле извлекаем цифры:
    - Инициализируем предыдущую цифру как -1 (значение, которое не может быть цифрой).
    - В каждой итерации:
        а) получаем текущую цифру (остаток от деления на 10);
        б) сравниваем текущую цифру с предыдущей (если предыдущая уже установлена и равна текущей, то нашли две одинаковые рядом -> выводим YES и выходим);
        в) обновляем предыдущую цифру = текущей;
        г) удаляем последнюю цифру (делим на 10).
 5. Если весь цикл прошел и не нашли совпадения, то выводим NO. */

    int number;          // Наше число
    int prev = -1;       // Предыдущая цифра (пока не было)
    int current;         // Текущая цифра

    scanf("%d", &number);
    
    // Если число отрицательное - делаем положительным
    if (number < 0) number = -number;
    
    // Специальный случай: если число 0
    if (number == 0) {
        printf("NO");    // У нуля всего одна цифра
        return 0;
    }
    
    while (number > 0) {          // Пока в числе есть цифры
        current = number % 10;    // Берем последнюю цифру (1)
        
        if (prev != -1) {         // Если была предыдущая цифра
            if (current == prev) {// Сравниваем текущую с предыдущей
                printf("YES");    // Нашли пару!
                return 0;         // Завершаем программу
            }
        }
        
        prev = current;           // Запоминаем текущую цифру (2)
        number = number / 10;     // Убираем последнюю цифру (3)
    }
    
    printf("NO");  // Если дошли сюда - пар не найдено
    return 0;
}