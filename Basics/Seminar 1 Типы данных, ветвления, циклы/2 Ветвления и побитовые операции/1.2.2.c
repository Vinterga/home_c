/******************************************************************************
Пример минимального числа
*******************************************************************************/
#include <stdio.h>

int main()
{
    int a, b, min;
    scanf("%d %d\n", &a, &b);
    
    min = (a < b) ? a : b;
    
    printf("%d\n", min = (a < b) ? a : b);
    
    return 0;
}
